% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_visualizations.R
\name{plot_cross_modal_coupling}
\alias{plot_cross_modal_coupling}
\title{Plot Cross-Modal Regulatory Coupling (RNA <-> ATAC)}
\usage{
plot_cross_modal_coupling(
  ref_rna,
  ref_atac,
  sim_rna,
  sim_atac,
  n_features = 20,
  palette = c("#2E86AB", "#E74C3C")
)
}
\arguments{
\item{ref_rna}{Reference scRNA-seq matrix (genes x cells).}

\item{ref_atac}{Reference scATAC-seq matrix (peaks x cells).}

\item{sim_rna}{Simulated scRNA-seq matrix (genes x cells).}

\item{sim_atac}{Simulated scATAC-seq matrix (peaks x cells).}

\item{n_features}{Integer. Number of linked gene-peak pairs to evaluate. Default \code{20}.}

\item{palette}{Character vector of 2 colors. Default \code{c("#2E86AB", "#E74C3C")}.}
}
\value{
A \code{ggplot} object of cross-modal correlation bars.
}
\description{
For multiomics benchmarking: compares Spearman cross-modality correlations between
linked gene expression (scRNA-seq) and chromatin accessibility (scATAC-seq) pairs.
}
\examples{
data(example_multiomics)
p <- plot_cross_modal_coupling(
  example_multiomics$ref_multi$rna, example_multiomics$ref_multi$atac,
  example_multiomics$sim_multi$rna, example_multiomics$sim_multi$atac
)
if (requireNamespace("ggplot2", quietly = TRUE)) print(p)
}
