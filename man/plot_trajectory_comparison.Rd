% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_visualizations.R
\name{plot_trajectory_comparison}
\alias{plot_trajectory_comparison}
\title{Plot Trajectory Dynamics Comparison (Pseudotime Q-Q Alignment)}
\usage{
plot_trajectory_comparison(
  ref_data,
  sim_data,
  palette = c("#2E86AB", "#E74C3C"),
  n_quantile_pts = 200
)
}
\arguments{
\item{ref_data}{Reference count matrix (genes x cells) or precomputed pseudotime vector.}

\item{sim_data}{Simulated count matrix (genes x cells) or precomputed pseudotime vector.}

\item{palette}{Character vector of 2 colors. Default \code{c("#2E86AB", "#E74C3C")}.}

\item{n_quantile_pts}{Integer. Quantile points for Q-Q plot. Default \code{200}.}
}
\value{
A \code{ggplot} object (or patchwork object if patchwork is available).
}
\description{
Compares continuous differentiation pseudotime distributions between reference and
simulated scRNA-seq data via quantile-quantile (Q-Q) alignment and overlaid KDE curves.
Pseudotime is auto-inferred from count matrices if not provided as precomputed vectors.
}
\examples{
data(example_scrna)
p <- plot_trajectory_comparison(example_scrna$ref, example_scrna$sim)
if (requireNamespace("ggplot2", quietly = TRUE)) print(p)
}
