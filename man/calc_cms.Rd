% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_metrics_batch.R
\name{calc_cms}
\alias{calc_cms}
\title{Cell-Specific Mixing Score (CMS)}
\usage{
calc_cms(
  data = NULL,
  batch_info,
  k = NULL,
  n_pcs = 30,
  coords = NULL,
  cell_min = 4,
  unbalanced = FALSE
)
}
\arguments{
\item{data}{Count or log-normalized expression matrix (features x cells) or NULL if coords provided.}

\item{batch_info}{Factor or character vector of batch assignments for each cell.}

\item{k}{Neighborhood size k for k-NN graph. Default is min(table(batch_info))/2 or 30.}

\item{n_pcs}{Number of principal components to compute if coords is NULL. Default is 30.}

\item{coords}{Optional precomputed embedding matrix (cells x dimensions, e.g. PCA or UMAP).}

\item{cell_min}{Minimum number of cells per batch in neighborhood to perform test. Default is 4.}

\item{unbalanced}{Logical, whether to set pure neighborhoods to NA. Default is FALSE.}
}
\value{
A named list:
  \item{cms_scores}{Numeric vector of CMS p-values for each cell (higher means better mixed)}
  \item{mean_cms}{Mean CMS score across cells}
  \item{median_cms}{Median CMS score across cells}
}
\description{
Tests the hypothesis that group-specific distance distributions of k-nearest
neighbor cells have the same underlying unspecified distribution.
Computes cell-specific p-values via Anderson-Darling tests (if kSamples
installed) or two-sample Kolmogorov-Smirnov / Kruskal-Wallis tests, with
optional delegation to CellMixS::cms.
}
